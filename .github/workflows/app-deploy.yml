
name: App CI/CD

on:
  push:
    branches: ["master"]

env:
  BACKEND_IMAGE: samarthfunde45/weather-backend
  FRONTEND_IMAGE: samarthfunde45/weather-frontend
  VERSION: v${{ github.run_number }}

jobs:

  # ──────────────────────────────────────────
  # JOB 1: Detect which files changed
  # ──────────────────────────────────────────
  detect-changes:
    name: Detect Changes
    runs-on: self-hosted
    outputs:
      backend:  ${{ steps.filter.outputs.backend }}
      frontend: ${{ steps.filter.outputs.frontend }}
      k8s:      ${{ steps.filter.outputs.k8s }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check which paths changed
        uses: dorny/paths-filter@v3
        id: filter
        with:
          base: master
          filters: |
            backend:
              - 'backend/**'
            frontend:
              - 'frontend/**'
            k8s:
              - 'k8s/**'


  # JOB 2: SonarCloud Code Quality Scan, Scans source code for bugs, vulnerabilities and code smells before building images.
  
  sonarcloud:
    name: SonarCloud Code Scan
    runs-on: self-hosted
    needs: detect-changes
    if: |
      needs.detect-changes.outputs.backend == 'true' ||
      needs.detect-changes.outputs.frontend == 'true'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # full history needed for SonarCloud blame

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}


  # JOB 3: Build & Push Backend
 
  build-backend:
    name: Build & Push Backend
    runs-on: self-hosted
    needs: [detect-changes, sonarcloud]
    if: |
      always() && !cancelled() &&
      needs.detect-changes.outputs.backend == 'true' &&
      needs.sonarcloud.result != 'failure'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push Backend Image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: |
            ${{ env.BACKEND_IMAGE }}:latest
            ${{ env.BACKEND_IMAGE }}:${{ env.VERSION }}

      # Trivy scans the image AFTER push for scan vulnerabilities
      - name: Trivy — Scan Backend Image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.BACKEND_IMAGE }}:${{ env.VERSION }}
          format: table
          exit-code: 0         # 0 = show results but don't fail pipeline
          severity: CRITICAL,HIGH

 
  # JOB 4: Build & Push Frontend
  
  build-frontend:
    name: Build & Push Frontend
    runs-on: self-hosted
    needs: [detect-changes, sonarcloud]
    if: |
      always() && !cancelled() &&
      needs.detect-changes.outputs.frontend == 'true' &&
      needs.sonarcloud.result != 'failure'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push Frontend Image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: |
            ${{ env.FRONTEND_IMAGE }}:latest
            ${{ env.FRONTEND_IMAGE }}:${{ env.VERSION }}

      # Trivy scans the image AFTER push for CVE vulnerabilities
      - name: Trivy — Scan Frontend Image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ env.FRONTEND_IMAGE }}:${{ env.VERSION }}
          format: table
          exit-code: 0         # 0 = show results but don't fail pipeline
          severity: CRITICAL,HIGH

  # ──────────────────────────────────────────
  # JOB 5: Deploy to Kubernetes
  # Runs when any build succeeds OR k8s files changed
  # ──────────────────────────────────────────
  deploy:
    name: Deploy to Kubernetes
    runs-on: self-hosted
    needs: [detect-changes, build-backend, build-frontend]
    if: |
      always() && !cancelled() &&
      (
        needs.build-backend.result  == 'success' ||
        needs.build-frontend.result == 'success' ||
        needs.detect-changes.outputs.k8s == 'true'
      )

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create App Secrets in Kubernetes
        run: |
          kubectl create secret generic app-secrets \
            --from-literal=MONGO_USER=${{ secrets.MONGO_USER }} \
            --from-literal=MONGO_PASS=${{ secrets.MONGO_PASS }} \
            --from-literal=MONGO_HOST=${{ secrets.MONGO_HOST }} \
            --from-literal=MONGO_PORT=${{ secrets.MONGO_PORT }} \
            --from-literal=MONGO_DB=${{ secrets.MONGO_DB }} \
            --from-literal=MONGO_AUTH_DB=${{ secrets.MONGO_AUTH_DB }} \
            --namespace=weather-app \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Apply Namespace
        run: kubectl apply -f k8s/namespace.yml

      - name: Apply App Manifests
        run: kubectl apply -f k8s/app/

      - name: Update Backend Image
        if: needs.build-backend.result == 'success'
        run: |
          kubectl set image deployment/backend \
            backend=${{ env.BACKEND_IMAGE }}:${{ env.VERSION }} \
            -n weather-app

      - name: Update Frontend Image
        if: needs.build-frontend.result == 'success'
        run: |
          kubectl set image deployment/frontend \
            frontend=${{ env.FRONTEND_IMAGE }}:${{ env.VERSION }} \
            -n weather-app

      - name: Wait for Backend Rollout
        if: needs.build-backend.result == 'success'
        run: kubectl rollout status deployment/backend -n weather-app --timeout=120s

      - name: Wait for Frontend Rollout
        if: needs.build-frontend.result == 'success'
        run: kubectl rollout status deployment/frontend -n weather-app --timeout=180s

      - name: Verify All Pods
        run: kubectl get pods -n weather-app
